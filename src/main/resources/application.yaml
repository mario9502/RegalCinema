spring:
  application:
    name: RegalCinemas
  jpa:
#    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: 'TRUE'
    hibernate:
      ddl-auto: update
    open-in-view: false
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: 'root'
    password: '12345'
    url: jdbc:mysql://localhost:3306/regal-cinemas?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true&serverTimezone=LOCAL
#  thymeleaf: # Thymeleaf
#    cache: false
#    mode: HTML
#    encoding: UTF-8
#    prefix: file:src/main/resources/templates/
#    resources: # Static resources
#      static-locations: file:src/main/resources/static/
#      cache:
#        period: 0
  thymeleaf:
    enabled: false
  sql:
    init:
      mode: never
#  devtools:
#    livereload:
#      enabled: true

logging:
  level:
    org:
      hibernate:
        type:
          descriptor: TRACE
        SQL: DEBUG
      nodeValue: WARN
    blog: WARN
server:
  port: 8080

movie:
  api:
    baseUrl: "http://localhost:8081"